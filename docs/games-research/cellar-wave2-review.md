# Cellar PRD Wave-2 Editorial Review — Gap Patch Summary

Reviewed all 12 wave-1 PRDs (`120-cellar-research-anchor.md` through `126-cellar-publish.md`) on 2026-05-10. Gaps found and patched: (1) 11 of 12 files had incorrect `parallelGroup` frontmatter values (e.g. `106`, `113`, `114`, `115`, `116`, `117`) — all fixed to match the NN prefix of each filename; (2) every cross-PRD reference that used "group NNN" or "groups NNN+" notation was replaced with the actual filename path so executors don't need to decode the numbering scheme; (3) `122-cellar-solver.md` was missing the `validateDeal` alias export contract (`{ winnable: boolean; reason?: string }`), the `< 500ms p95` runtime budget on mid-range 2024 mobile, the complete list of 8 known-unsolvable Microsoft deal numbers, and the fc-solve attribution (Shlomi Fish, MIT/Expat) — all inlined; (4) `123-cellar-state-persistence.md` was missing the complete `bilko_cellar_*` localStorage key table (10 keys with schema-version and survives-reload columns) and the "what survives a hard reload" vs "what does not survive" inventory — added; (5) `123-cellar-ui-board.md` was missing the 6-token CSS card-game variable table, the full pointer-event drag-and-drop handler sequence (pointerdown → 4px threshold → pointermove ghost → pointerup `positionAtPoint`), the mobile 350ms long-press initiation, the tap-tap screen-reader fallback, and explicit 80ms/step 40ms-stagger supermove timing with `prefers-reduced-motion` override — all added; (6) `123-cellar-ui-hud.md` was missing the 13-element aria-label table and the daily-mode restart confirm copy variant — added; (7) `124-cellar-daily-deal.md` was missing the FNV-1a deterministic deal# derivation code, the explicit day-of-week difficulty ramp (Mon/Tue=easy, Wed/Thu=standard, Fri=hard, Sat/Sun=master), the share-tile string format (7 fields, 3 square encodings), and the 5-state calendar-archive cell spec — all added; (8) `124-cellar-deal-picker.md` was missing the solver-cache `cachedClassify` Map implementation, the "play random picks an unplayed deal (up to 20 re-rolls)" logic, and a note that the valid range is `[1, 1_000_000]` not the narrower 32000-deal Microsoft subset — added; (9) `124-cellar-themes-a11y.md` was missing the complete 8-variable × 4-theme CSS token table, the exact `::after` pseudo-element colorblind glyph differentiator code (triangle for ♥♦, circle for ♣♠), and the `prefers-reduced-motion` complete override list covering all 6 CSS files — added; (10) `125-cellar-animations-haptics.md` was missing the 12-row complete animation catalog and the 8-row sound + haptic catalog with SFX names, durations, frequencies, volume factors, and vibrate patterns — both tables added along with master-controls section; (11) `126-cellar-publish.md` was missing the exact bundle-budget SQL for the `app_budgets` table (307200 bytes = 300 KB gzip), "Solitaire" in the registry tags array, and a dual-remote push reminder — all added; (12) `Position.kind` canonical spelling standardized to `'cell'` (not `'free'`) across all PRDs with an explicit note in `121-cellar-bootstrap.md` and `122-cellar-engine.md`. No open questions remain; all acceptance criteria are unblocked for fresh-executor runs.
